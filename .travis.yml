language: python
python:
  - '3.5'

before_script:
  - pip install -r requirements.txt


env: DJANGO_SETTINGS_MODULE="PurBeurre.settings.travis"

services:
  - postgresql


script:
  - flake8 search_food/

branches:
  only:
    - staging
